import $ from "jquery";
import bootstrap from "bootstrap";

const CTA_TIMEOUT = 10 * 1000;

$(document).ready( () => {
    /*
    setTimeout( () => {
        $('#CTAModal').modal();
    }, CTA_TIMEOUT);
    */
});